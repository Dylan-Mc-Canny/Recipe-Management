<style>
    
</style>
@if (User.Identity.IsAuthenticated)
{
    <!-- Logged in -->
    <div class="navbar-nav" >  
        <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi-person-fill text-light"></i> @User.Identity.Name
            </a>
            <div class="dropdown-menu dropdown-menu-right bg-dark" aria-labelledby="navbarDropdownMenuLink">
                <form asp-controller="User" asp-action="Logout">
                    <button type="submit" class="dropdown-item btn btn-link bg-dark text-light"><i class="bi bi-box-arrow-right text-light"></i> Logout</button>
                </form>
            </div>
        </div> 
    </div>
}
else 
{
    <!-- Not Logged in -->
    <div class="navbar-nav">
        <a class="nav-link text-light"  asp-controller="User" asp-action="Login">Login</a>
        <a class="nav-link text-light"  asp-controller="User" asp-action="Register">Register</a>    
    </div>
}

